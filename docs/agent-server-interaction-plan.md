# Agent-Server 交互功能 - 细粒度任务拆分

## 📊 任务概览

```
总计：9 个大任务 → 拆分为 43 个子任务
阶段一（基础设施）：3 个大任务 → 13 个子任务
阶段二（通信层）：2 个大任务 → 12 个子任务
阶段三（状态上报）：2 个大任务 → 10 个子任务
阶段四（集成）：2 个大任务 → 8 个子任务

进度统计：
✅ 已完成：19 个子任务
🔄 进行中：0 个子任务
⏳ 待开始：24 个子任务
```

---

## 🔧 阶段一：基础设施

### 📦 任务1：agent 模块（5 个子任务）

| 子任务 | 任务名称 | 状态 | 预估时间 |
|--------|---------|------|---------|
| 1.1 | 实现 Agent ID 生成 - DMI 和 MAC 地址 | ✅ 已完成 | 2 小时 |
| 1.2 | 实现 Agent ID 生成 - machine-id 回退 | ✅ 已完成 | 1.5 小时 |
| 1.3 | 实现 Agent ID 持久化 | ✅ 已完成 | 0.5 小时 |
| 1.4 | 实现 Agent 包初始化 | ✅ 已完成 | 0.5 小时 |
| 1.5 | 实现状态管理 | ✅ 已完成 | 1 小时 |

#### 1.1 实现 Agent ID 生成 - DMI 和 MAC 地址
- **功能**：基于硬件信息生成 Agent ID
- **文件**：`agent/id.go`
- **核心逻辑**：
  - 读取系统 DMI 信息（UUID、产品名称等）
  - 读取网络接口 MAC 地址
  - 过滤无效的硬件信息（如虚拟机的默认值）
  - 基于多个硬件信息源生成唯一 ID
- **测试**：在不同硬件环境下验证 ID 生成逻辑
- **预估时间**：2 小时

#### 1.2 实现 Agent ID 生成 - machine-id 回退
- **功能**：从系统 machine-id 生成 Agent ID（回退方案）
- **文件**：`agent/id.go`
- **核心逻辑**：
  - 读取系统 machine-id 文件（优先尝试标准 UUID 格式）
  - 如果格式不符合标准，基于文件内容生成 ID
  - 读取本地持久化的 machine-id 文件
  - 最后回退：生成全新的 UUID
- **测试**：验证各种回退场景（文件不存在、格式错误等）
- **预估时间**：1.5 小时

#### 1.3 实现 Agent ID 持久化
- **功能**：将生成的 Agent ID 持久化到文件
- **文件**：`agent/id.go`
- **核心逻辑**：
  - 生成 ID 后写入本地文件
  - 设置合适的文件权限
  - 确保工作目录存在
- **测试**：验证文件创建和权限设置
- **预估时间**：0.5 小时

#### 1.4 实现 Agent 包初始化
- **功能**：初始化 agent 包的基础变量和 context
- **文件**：`agent/id.go`, `agent/state.go`
- **核心逻辑**：
  - 定义 Agent 运行所需的基础变量（ID、工作目录、产品信息、版本等）
  - 初始化全局 context 用于协调各模块退出
  - 在包初始化时调用 ID 生成逻辑
- **预估时间**：0.5 小时

#### 1.5 实现状态管理
- **功能**：管理 Agent 运行状态（running/abnormal）
- **文件**：`agent/state.go`
- **核心逻辑**：
  - 定义 Agent 状态类型（运行中、异常）
  - 提供状态设置接口（设置为运行中、设置为异常）
  - 提供状态查询接口（返回当前状态和错误信息）
  - 确保状态操作的线程安全
- **测试**：并发测试状态切换的线程安全性
- **预估时间**：1 小时

**任务1 总计：5.5 小时**

---

### 📦 任务2：host 模块（4 个子任务）

| 子任务 | 任务名称 | 状态 | 预估时间 |
|--------|---------|------|---------|
| 2.1 | 实现主机名获取 | ✅ 已完成 | 0.5 小时 |
| 2.2 | 实现 IPv4 地址收集 | ✅ 已完成 | 1.5 小时 |
| 2.3 | 实现平台信息收集 | ✅ 已完成 | 1.5 小时 |
| 2.4 | 实现 host 包初始化 | ✅ 已完成 | 0.5 小时 |

#### 2.1 实现主机名获取
- **功能**：获取并存储主机名
- **文件**：`host/host.go`
- **核心逻辑**：
  - 获取系统主机名
  - 提供主机名刷新接口
- **测试**：验证主机名获取
- **预估时间**：0.5 小时

#### 2.2 实现 IPv4 地址收集
- **功能**：收集主机的所有 IPv4 地址
- **文件**：`host/host.go`
- **核心逻辑**：
  - 遍历系统网络接口
  - 过滤掉虚拟接口（如 docker、loopback）
  - 收集所有有效的 IPv4 地址
  - 限制收集数量避免过多
- **测试**：验证 IP 收集逻辑（多网卡、无 IP 等场景）
- **预估时间**：1.5 小时

#### 2.3 实现平台信息收集
- **功能**：收集系统平台信息（架构、内核版本、发行版等）
- **文件**：`host/platform.go`
- **核心逻辑**：
  - 获取操作系统平台信息（发行版名称、版本等）
  - 获取系统架构信息
  - 获取内核版本信息
  - 在包初始化时收集这些信息
- **测试**：在不同 Linux 发行版上验证信息准确性
- **预估时间**：1.5 小时

#### 2.4 实现 host 包初始化
- **功能**：初始化 host 包并实现定期刷新机制
- **文件**：`host/host.go`
- **核心逻辑**：
  - 定义主机信息存储变量
  - 实现主机信息刷新函数（整合主机名和 IP 收集）
  - 在包初始化时执行首次刷新
- **预估时间**：0.5 小时

**任务2 总计：4 小时**

---

### 📦 任务3：config 模块（4 个子任务）

| 子任务 | 任务名称 | 状态 | 预估时间 |
|--------|---------|------|---------|
| 3.1 | 定义配置结构体 | ✅ 已完成 | 0.5 小时 |
| 3.2 | 实现配置文件读取 | ✅ 已完成 | 1.5 小时 |
| 3.3 | 实现配置验证和默认值 | ✅ 已完成 | 1 小时 |
| 3.4 | 实现全局配置访问 | ✅ 已完成 | 0.5 小时 |

#### 3.1 定义配置结构体
- **功能**：定义配置数据结构
- **文件**：`config/config.go`
- **核心逻辑**：
  - 定义配置结构体（Server 地址、超时时间、Agent 工作目录等）
  - 定义全局配置变量
- **预估时间**：0.5 小时

#### 3.2 实现配置文件读取
- **功能**：从 YAML 文件读取配置
- **文件**：`config/config.go`
- **核心逻辑**：
  - 支持从配置文件路径读取配置
  - 支持 YAML 格式解析
  - 配置文件路径支持多种方式（环境变量、默认路径、当前目录）
  - 处理配置文件读取错误
- **测试**：验证配置文件读取和路径优先级
- **预估时间**：1.5 小时

#### 3.3 实现配置验证和默认值
- **功能**：验证配置有效性并设置默认值
- **文件**：`config/config.go`
- **核心逻辑**：
  - 验证必填配置项是否存在
  - 为可选配置项设置合理的默认值
  - 配置验证失败时返回明确的错误信息
- **测试**：验证默认值和错误处理
- **预估时间**：1 小时

#### 3.4 实现全局配置访问
- **功能**：提供全局配置访问接口
- **文件**：`config/config.go`
- **核心逻辑**：
  - 提供配置初始化接口（在程序启动时调用）
  - 提供配置获取接口（供其他模块使用）
  - 确保配置在使用前已初始化
- **测试**：验证全局配置访问和初始化检查
- **预估时间**：0.5 小时

**任务3 总计：3.5 小时**

---

## 🔧 阶段二：通信层

### 📦 任务4：proto 模块完善（6 个子任务）

| 子任务 | 任务名称 | 状态 | 预估时间 |
|--------|---------|------|---------|
| 4.1 | 定义基础消息类型 | ✅ 已完成 | 0.5 小时 |
| 4.2 | 定义 PackagedData 消息 | ✅ 已完成 | 0.5 小时 |
| 4.3 | 定义 Command 消息 | ✅ 已完成 | 0.5 小时 |
| 4.4 | 定义 Transfer 服务 | ✅ 已完成 | 0.5 小时 |
| 4.5 | 生成 Go 代码 | ✅ 已完成 | 1 小时 |
| 4.6 | 验证 proto 定义完整性 | ✅ 已完成 | 0.5 小时 |

#### 4.1 定义基础消息类型
- **功能**：定义 protobuf 基础消息
- **文件**：`proto/grpc.proto`
- **核心逻辑**：
  - 定义编码后的记录消息（包含数据类型、时间戳、数据内容）
  - 定义未编码的记录消息（包含数据类型、时间戳、数据负载）
  - 定义数据负载消息（键值对格式）
- **预估时间**：0.5 小时

#### 4.2 定义 PackagedData 消息
- **功能**：定义 Agent 发送给 Server 的数据包
- **文件**：`proto/grpc.proto`
- **核心逻辑**：
  - 定义数据包消息结构
  - 包含采集的数据记录列表
  - 包含 Agent 标识信息（ID、版本、产品名）
  - 包含主机信息（主机名、IP 地址列表）
- **预估时间**：0.5 小时

#### 4.3 定义 Command 消息
- **功能**：定义 Server 下发给 Agent 的命令
- **文件**：`proto/grpc.proto`
- **核心逻辑**：
  - 定义命令消息结构
  - 包含任务信息（可选）
  - 包含插件配置信息（可选）
  - 确认相关消息类型已定义
- **预估时间**：0.5 小时

#### 4.4 定义 Transfer 服务
- **功能**：定义 gRPC 双向流服务
- **文件**：`proto/grpc.proto`
- **核心逻辑**：
  - 定义传输服务接口
  - 支持双向流通信（Agent 发送数据，Server 下发命令）
- **预估时间**：0.5 小时

#### 4.5 生成 Go 代码
- **功能**：使用 protoc 生成 Go 代码
- **文件**：`proto/grpc.pb.go`, `proto/grpc_grpc.pb.go`（生成的文件）
- **核心逻辑**：
  - 安装 protobuf 编译工具和 Go 插件
  - 编写代码生成命令或脚本
  - 生成 Go 代码文件
  - 验证生成的代码可以编译
- **测试**：验证生成的代码可以正常编译和使用
- **预估时间**：1 小时

#### 4.6 验证 proto 定义完整性
- **功能**：验证所有消息定义完整且正确
- **文件**：`proto/grpc.proto`
- **核心逻辑**：
  - 检查所有消息字段是否完整
  - 确认字段编号不冲突
  - 编写简单的序列化/反序列化测试
  - 确认与现有代码的兼容性
- **测试**：验证消息序列化和反序列化
- **预估时间**：0.5 小时

**任务4 总计：3.5 小时**

---

### 📦 任务5：transport 模块（6 个子任务）

| 子任务 | 任务名称 | 状态 | 预估时间 |
|--------|---------|------|---------|
| 5.1 | 实现基础连接管理 | ⏳ 待开始 | 2 小时 |
| 5.2 | 实现连接重试机制 | ⏳ 待开始 | 1.5 小时 |
| 5.3 | 实现数据发送逻辑 | ⏳ 待开始 | 3 小时 |
| 5.4 | 实现命令接收逻辑 | ⏳ 待开始 | 3 小时 |
| 5.5 | 实现传输守护进程 | ⏳ 待开始 | 2 小时 |
| 5.6 | 实现 transport 包初始化 | ⏳ 待开始 | 0.5 小时 |

#### 5.1 实现基础连接管理
- **功能**：建立和管理 gRPC 连接
- **文件**：`transport/connection.go`
- **核心逻辑**：
  - 提供连接获取接口
  - 从配置中读取 Server 地址
  - 建立 gRPC 连接（无 TLS 加密）
  - 检查连接状态（就绪、连接中、失败等）
  - 管理连接的生命周期
- **测试**：验证基础连接建立和状态检查
- **预估时间**：2 小时

#### 5.2 实现连接重试机制
- **功能**：连接失败时自动重试
- **文件**：`transport/connection.go`
- **核心逻辑**：
  - 连接失败时关闭旧连接
  - 实现重试计数逻辑（限制最大重试次数）
  - 设置重试间隔
  - 连接成功后重置重试计数
  - 超过最大重试次数后返回错误
- **测试**：模拟 Server 不可用场景，验证重试逻辑
- **预估时间**：1.5 小时

#### 5.3 实现数据发送逻辑
- **功能**：将采集到的数据发送到 Server
- **文件**：`transport/transfer.go`
- **核心逻辑**：
  - 从数据缓冲区读取待发送的记录
  - 将记录打包成数据包，包含：
    - 采集的数据记录
    - Agent 标识信息（ID、版本、产品名）
    - 主机信息（主机名、IP 地址）
  - 通过 gRPC 双向流发送数据包到 Server
  - 发送成功后释放缓冲区资源
  - 定期检查缓冲区，有新数据时及时发送
- **错误处理**：
  - 发送失败时记录错误日志
  - 连接断开时触发重连机制
- **测试**：验证数据发送流程、错误处理
- **预估时间**：3 小时

#### 5.4 实现命令接收逻辑
- **功能**：接收并处理 Server 下发的命令
- **文件**：`transport/transfer.go`
- **核心逻辑**：
  - 接收 Server 下发的命令消息
  - 处理任务命令：
    - Agent 自身的任务（当前无具体任务）
    - 转发给对应插件的任务
  - 处理配置命令：
    - 同步插件配置信息
  - 更新 Agent 运行状态
  - 处理接收过程中的错误
- **测试**：验证命令接收和处理逻辑
- **预估时间**：3 小时

#### 5.5 实现传输守护进程
- **功能**：启动和管理传输守护进程
- **文件**：`transport/transfer.go`, `transport/transport.go`
- **核心逻辑**：
  - 实现传输主循环（持续尝试连接和传输）
  - 连接成功后启动数据发送和接收的并发处理
  - 连接断开后等待一段时间后重试
  - 超过最大重试次数后退出（触发 Agent 关闭）
  - 提供守护进程启动接口
- **测试**：验证守护进程启动、重连和退出逻辑
- **预估时间**：2 小时

#### 5.6 实现 transport 包初始化
- **功能**：初始化 transport 包的变量和依赖
- **文件**：`transport/connection.go`
- **核心逻辑**：
  - 定义连接管理相关的包级变量
  - 初始化重试计数等状态变量
- **预估时间**：0.5 小时

**任务5 总计：12 小时**

---

## 🔧 阶段三：状态上报

### 📦 任务6：resource 模块（5 个子任务）

| 子任务 | 任务名称 | 状态 | 预估时间 |
|--------|---------|------|---------|
| 6.1 | 实现进程资源监控基础功能 | ⏳ 待开始 | 2 小时 |
| 6.2 | 实现 IO 速度计算 | ⏳ 待开始 | 2 小时 |
| 6.3 | 实现系统资源获取 | ⏳ 待开始 | 1.5 小时 |
| 6.4 | 实现 Linux 特定功能 | ⏳ 待开始 | 2 小时 |
| 6.5 | 实现 resource 包初始化 | ⏳ 待开始 | 0.5 小时 |

#### 6.1 实现进程资源监控基础功能
- **功能**：获取进程的基础资源使用情况
- **文件**：`resource/resource.go`
- **核心逻辑**：
  - 提供进程资源查询接口
  - 获取进程 CPU 使用率
  - 获取进程内存使用量
  - 获取进程文件描述符数量
  - 获取进程启动时间
  - 处理进程不存在、权限不足等错误情况
- **测试**：验证进程资源获取的准确性
- **预估时间**：2 小时

#### 6.2 实现 IO 速度计算
- **功能**：计算进程的读写 IO 速度
- **文件**：`resource/resource.go`
- **核心逻辑**：
  - 获取进程 IO 统计信息
  - 使用缓存机制存储上次 IO 状态
  - 计算读写速度（基于时间差和 IO 增量）
  - 首次计算时使用进程启动时间作为基准
- **测试**：验证 IO 速度计算的准确性（包括首次和后续计算）
- **预估时间**：2 小时

#### 6.3 实现系统资源获取
- **功能**：获取系统级别的资源信息
- **文件**：`resource/resource.go`
- **核心逻辑**：
  - 获取系统总内存大小
  - 获取 CPU 型号信息
  - 获取系统启动时间
  - 计算指定目录的大小（排除指定子目录）
- **测试**：验证系统资源获取和目录大小计算
- **预估时间**：1.5 小时

#### 6.4 实现 Linux 特定功能
- **功能**：获取 Linux 系统特定的信息
- **文件**：`resource/resource_linux.go`
- **核心逻辑**：
  - 读取系统 DNS 配置信息
  - 读取系统默认网关信息
  - 读取系统硬件信息（序列号、UUID、型号、厂商等）
  - 在包初始化时收集这些信息
- **测试**：验证 Linux 特定功能的准确性
- **预估时间**：2 小时

#### 6.5 实现 resource 包初始化
- **功能**：初始化 resource 包的变量和缓存
- **文件**：`resource/resource.go`
- **核心逻辑**：
  - 定义系统资源相关的包级变量
  - 初始化 IO 状态缓存和进程缓存
  - 在包初始化时调用系统资源获取函数
- **预估时间**：0.5 小时

**任务6 总计：8 小时**

---

### 📦 任务7：heartbeat 模块（5 个子任务）

| 子任务 | 任务名称 | 状态 | 预估时间 |
|--------|---------|------|---------|
| 7.1 | 实现 Agent 状态收集 - 基础信息 | ⏳ 待开始 | 1.5 小时 |
| 7.2 | 实现 Agent 状态收集 - 性能指标 | ⏳ 待开始 | 2 小时 |
| 7.3 | 实现 Agent 状态收集 - 网络和负载 | ⏳ 待开始 | 2 小时 |
| 7.4 | 实现 Plugin 状态收集 | ⏳ 待开始 | 2.5 小时 |
| 7.5 | 实现心跳守护进程 | ⏳ 待开始 | 1 小时 |

#### 7.1 实现 Agent 状态收集 - 基础信息
- **功能**：收集 Agent 的基础状态信息
- **文件**：`heartbeat/heartbeat.go`
- **核心逻辑**：
  - 创建 Agent 状态记录
  - 收集基础信息（内核版本、架构、平台信息等）
  - 收集 Agent 运行状态（运行中/异常）
  - 收集 Agent 进程 ID
  - 将状态信息写入数据缓冲区
- **测试**：验证基础信息收集的准确性
- **预估时间**：1.5 小时

#### 7.2 实现 Agent 状态收集 - 性能指标
- **功能**：收集 Agent 的性能指标和系统信息
- **文件**：`heartbeat/heartbeat.go`
- **核心逻辑**：
  - 获取 Agent 进程的资源使用情况（CPU、内存、IO 等）
  - 获取系统级别的资源信息（总内存、CPU 型号、启动时间等）
  - 获取系统 CPU 和内存使用率
  - 获取运行时信息（goroutine 数量、CPU 核心数等）
  - 将性能指标添加到状态记录中
- **测试**：验证性能指标收集的准确性
- **预估时间**：2 小时

#### 7.3 实现 Agent 状态收集 - 网络和负载
- **功能**：收集网络信息和系统负载
- **文件**：`heartbeat/heartbeat.go`
- **核心逻辑**：
  - 收集网络传输统计信息（当前版本可设为 0）
  - 收集系统负载信息（Linux 系统读取负载平均值）
  - 收集插件目录大小信息
  - 将网络和负载信息添加到状态记录中
- **测试**：验证网络和负载信息收集（包括非 Linux 系统）
- **预估时间**：2 小时

#### 7.4 实现 Plugin 状态收集
- **功能**：收集所有 Plugin 的状态信息
- **文件**：`heartbeat/heartbeat.go`
- **核心逻辑**：
  - 遍历所有已加载的插件
  - 跳过已退出的插件
  - 为每个插件创建状态记录
  - 收集插件基础信息（名称、版本）
  - 收集插件资源使用情况（CPU、内存、IO 等）
  - 收集插件目录大小
  - 收集插件传输统计信息
  - 将插件状态信息写入数据缓冲区
- **测试**：验证 Plugin 状态收集的准确性
- **预估时间**：2.5 小时

#### 7.5 实现心跳守护进程
- **功能**：启动心跳守护进程，定期上报状态
- **文件**：`heartbeat/heartbeat.go`
- **核心逻辑**：
  - 提供心跳守护进程启动接口
  - 启动时立即执行一次状态收集
  - 启动定时器，定期执行状态收集
  - 定期刷新主机信息
  - 等待退出信号后优雅退出
- **测试**：验证心跳守护进程正常运行和退出
- **预估时间**：1 小时

**任务7 总计：9 小时**

---

## 🔧 阶段四：集成

### 📦 任务8：buffer 模块完善（3 个子任务）

| 子任务 | 任务名称 | 状态 | 预估时间 |
|--------|---------|------|---------|
| 8.1 | 实现 WriteRecord 方法 | ⏳ 待开始 | 1 小时 |
| 8.2 | 实现对象池优化 | ⏳ 待开始 | 1.5 小时 |
| 8.3 | 优化 buffer 写入逻辑 | ⏳ 待开始 | 0.5 小时 |

#### 8.1 实现 WriteRecord 方法
- **功能**：支持写入未编码的 Record
- **文件**：`buffer/buffer.go`
- **核心逻辑**：
  - 提供写入未编码记录的接口
  - 将未编码记录转换为编码格式
  - 序列化记录数据
  - 写入数据缓冲区
  - 处理序列化失败、缓冲区溢出等错误
- **测试**：验证 WriteRecord 功能和错误处理
- **预估时间**：1 小时

#### 8.2 实现对象池优化
- **功能**：使用对象池减少内存分配
- **文件**：`buffer/pool.go`
- **核心逻辑**：
  - 提供从对象池获取记录对象的接口
  - 提供归还对象到对象池的接口
  - 根据数据大小创建合适大小的对象
- **测试**：验证对象池功能和内存使用优化
- **预估时间**：1.5 小时

#### 8.3 优化 buffer 写入逻辑
- **功能**：优化 buffer 满时的处理逻辑
- **文件**：`buffer/buffer.go`
- **核心逻辑**：
  - 实现缓冲区满时的处理策略（覆盖最旧数据）
  - 添加缓冲区溢出时的错误处理
  - 优化并发访问的锁机制
- **测试**：验证 buffer 溢出场景
- **预估时间**：0.5 小时

**任务8 总计：3 小时**

---

### 📦 任务9：main.go 完善（5 个子任务）

| 子任务 | 任务名称 | 状态 | 预估时间 |
|--------|---------|------|---------|
| 9.1 | 实现 logger 初始化 | ⏳ 待开始 | 1.5 小时 |
| 9.2 | 实现配置初始化 | ⏳ 待开始 | 0.5 小时 |
| 9.3 | 实现 pid 文件锁 | ⏳ 待开始 | 1 小时 |
| 9.4 | 实现守护进程启动 | ⏳ 待开始 | 1.5 小时 |
| 9.5 | 实现信号处理和清理 | ⏳ 待开始 | 1 小时 |

#### 9.1 实现 logger 初始化
- **功能**：初始化日志系统并配置日志输出
- **文件**：`main.go`
- **核心逻辑**：
  - 配置日志输出（文件日志）
  - 实现日志轮转功能
  - 根据环境变量设置日志级别
  - 替换全局日志实例
- **测试**：验证日志输出和轮转功能
- **预估时间**：1.5 小时

#### 9.2 实现配置初始化
- **功能**：初始化全局配置
- **文件**：`main.go`
- **核心逻辑**：
  - 调用配置初始化接口
  - 验证配置有效性（如果无效则退出）
  - 设置 Agent 工作目录
  - 记录配置信息到日志
- **测试**：验证配置加载和错误处理
- **预估时间**：0.5 小时

#### 9.3 实现 pid 文件锁
- **功能**：防止 Agent 重复启动
- **文件**：`main.go`
- **核心逻辑**：
  - 使用文件锁机制防止重复启动
  - 支持通过环境变量配置 pid 文件路径
  - 启动时加锁，退出时释放
  - 如果锁已被占用，记录错误并退出
- **测试**：验证重复启动保护
- **预估时间**：1 小时

#### 9.4 实现守护进程启动
- **功能**：启动所有守护进程并协调运行
- **文件**：`main.go`
- **核心逻辑**：
  - 创建全局 context 用于协调各模块退出
  - 创建 WaitGroup 用于等待所有守护进程退出
  - 启动心跳守护进程
  - 启动插件管理守护进程
  - 启动传输守护进程
  - 记录启动信息到日志
  - 等待所有守护进程退出
- **测试**：验证所有守护进程正常启动和协调运行
- **预估时间**：1.5 小时

#### 9.5 实现信号处理和清理
- **功能**：处理退出信号并清理资源
- **文件**：`main.go`
- **核心逻辑**：
  - 监听退出信号
  - 收到信号后记录日志
  - 等待一段时间让守护进程优雅退出
  - 取消全局 context 触发各模块退出
  - 清理临时文件
  - 释放 pid 文件锁
  - 记录退出日志
- **测试**：验证优雅退出流程和资源清理
- **预估时间**：1 小时

**任务9 总计：5.5 小时**

---

## 📊 总览统计

| 阶段 | 大任务数 | 子任务数 | 预估总时间 |
|------|---------|---------|-----------|
| 阶段一：基础设施 | 3 | 13 | 13 小时 |
| 阶段二：通信层 | 2 | 12 | 15.5 小时 |
| 阶段三：状态上报 | 2 | 10 | 17 小时 |
| 阶段四：集成 | 2 | 8 | 8.5 小时 |
| **总计** | **9** | **43** | **54 小时** |

---

## 📝 任务分配建议

### 开发人员 A（基础设施）
- 任务1：agent 模块（5.5 小时）
- 任务2：host 模块（4 小时）
- 任务3：config 模块（3.5 小时）
- **小计：13 小时**

### 开发人员 B（通信层）
- 任务4：proto 模块（3.5 小时）
- 任务5：transport 模块（12 小时）
- **小计：15.5 小时**

### 开发人员 C（状态上报）
- 任务6：resource 模块（8 小时）
- 任务7：heartbeat 模块（9 小时）
- **小计：17 小时**

### 开发人员 D（集成）
- 任务8：buffer 模块（3 小时）
- 任务9：main.go（5.5 小时）
- **小计：8.5 小时**

---

## 📌 需求确认

### ✅ 需要实现的功能
- gRPC 双向流通信（无加密）
- 配置文件读取（Server 地址等）
- Agent 状态上报
- Plugin 状态上报
- Agent ID 生成
- 状态管理
- 主机信息收集
- pid 文件锁

### ❌ 不需要的功能
- 多区域支持
- 服务发现
- TLS/mTLS 认证
- Snappy 压缩
- 文件上传
- Agent 自更新
- pprof 调试 / 手动内存释放
- systemd watchdog
- 日志上报到 Server
- IPv6 地址收集
- IP 地址内网/公网判断
- 性能要求

---

## 📝 计划编写原则

1. **聚焦功能目标**：每个任务明确"做什么"和"为什么"
2. **去掉实现细节**：不包含具体函数名、API 调用、代码级细节
3. **描述业务逻辑**：关注功能目标和业务需求，而非实现方式
4. **项目初始阶段**：不包含性能要求
5. **提升可读性**：计划简洁，便于理解和执行
